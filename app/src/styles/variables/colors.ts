export const colors = {
    black: (opacity = 1) =>
      `rgba(0, 0, 0, ${opacity})`, // #000000

    primary_dark: (opacity = 1) =>
      `rgba(101, 101, 101, ${opacity})`, // 

    raisin_black: (opacity = 1) =>
      `rgba(38, 38, 38, ${opacity})`, // #262626
  
    jet: (opacity = 1) =>
      `rgba(51, 51, 51, ${opacity})`, // #333333

    spanish_gray: (opacity = 1) =>
      `rgba(153, 153, 153, ${opacity})`, // #999999
  
    taupe_gray: (opacity = 1) =>
      `rgba(135, 135, 135, ${opacity})`, // #878787
  
    gainsboro: (opacity = 1) =>
      `rgba(222, 222, 222, ${opacity})`, // #DEDEDE
  
    platinum: (opacity = 1) =>
      `rgba(228, 228, 228, ${opacity})`, // #E4E4E4
  
    light_platinum: (opacity = 1) =>
      `rgba(231, 231, 231, ${opacity})`, // #E7E7E7
  
    white_smoke: (opacity = 1) =>
      `rgba(246, 246, 246, ${opacity})`, // #F6F6F6
  
    snow: (opacity = 1) =>
      `rgba(249, 249, 249, ${opacity})`, // #F9F9F9
  
    white: (opacity = 1) =>
      `rgba(255, 255, 255, ${opacity})`, // #FFFFFF

    dark: '#333333'
  };
